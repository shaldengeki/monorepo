load("//tools/build_rules/api/migrations:defs.bzl", "migrations")

migrations(
    app_config = "//proto_registry:config_py",
    app_package = "proto_registry.config",
    versions = glob(["versions/*.py"]),
)

import React from 'react';

type ExampleComponentProps = {
}

const ExampleComponent = ({}: ExampleComponentProps) => {
    return <div>
        <p>Example component</p>
    </div>
};

export default ExampleComponent;
